package com.nathanielbennett.tweeter.model.service.request;

import java.io.Serializable;

public interface TweeterAPIRequest {

    // Once we need to have specific ways to extract request data to turn into AWS API calls
    // (such as implementing Serializable), we can add them here

}
